name: Deploy tenant-69db4313-ffe9-51df-9735-785e962a7af3 Site to Firebase Hosting

on:
  push:
    branches: [ main, master ]
    paths:
      - 'index.html'
      - 'assets/**'
      - 'config/**'
  pull_request:
    branches: [ main, master ]
    paths:
      - 'index.html'
      - 'assets/**'
      - 'config/**'
  workflow_dispatch:

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20'
        
    - name: Deploy to Firebase Hosting
      uses: FirebaseExtended/action-hosting-deploy@v0
      with:
        firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT }}'
        projectId: einar-404623
        channelId: 69db4313-ffe9-51df-9735-785e962a7af3
        entryPoint: .
        target: transport-app-agents
